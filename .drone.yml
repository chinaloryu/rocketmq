pipeline:
  dingtalk:
    name: dingtalk
    image: guoxudongdocker/drone-dingtalk 
    settings:
      token: 
        from_secret: DINGTALK_TOKEN
      type: markdown
      message_color: true
      message_pic: true
      sha_link: true
  when:
    status: [failure, success]
